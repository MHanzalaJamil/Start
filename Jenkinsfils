pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                git 'https://github.com/MHanzalaJamil/Start.git'
            }
        }
    }
    post {
        success {
            echo "success"
        }
        failure {
            echo "failure"
        }
     }
        
}
